import { Box } from '@mui/material'

const ChatMessages = () => {
    return (
        <Box sx={{
            display: 'flex',
            flexGrow: 1
        }}>
            
        </Box>
    );
}

export default ChatMessages
